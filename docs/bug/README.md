# Bug Tracker

Per-bug folders created by the `/dev-bug` skill. Each subdirectory is one
incident or reproducible bug, with an `INDEX.md` describing the issue and
working notes. Compact AI summaries live under `.ai/`.

See `.agents/skills/dev-bug/skill.md` for the full lifecycle.
